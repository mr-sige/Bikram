@model Bikram.Models.BikramViewModel
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    @{
           for (int i = 0; i < @Model.TodaysClasses.Count; i++)
           {
               <p>@Model.TodaysClasses[i]</p>
                
           }
       }
   
</div>

